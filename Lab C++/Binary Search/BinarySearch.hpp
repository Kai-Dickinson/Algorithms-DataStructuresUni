#include <cstddef>
#include <math.h>
#include <vector>
#include <iostream>

class BinarySearch {
public:
  bool search(int *, int, size_t, size_t);
    int count(int *array, int key, size_t lo, size_t hi);
    int counting = 0;
};
